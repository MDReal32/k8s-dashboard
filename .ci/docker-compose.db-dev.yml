version: "3"

services:
  db:
    image: postgres:latest
    ports:
      - "9001:5432"
    env_file:
      - ../.env
    volumes:
      - postgres:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  postgres: