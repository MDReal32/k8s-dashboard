{
  "name": "plugin-helm",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "plugins/plugin-helm/src",
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "main": "plugins/plugin-helm/src/index.ts",
        "tsConfig": "plugins/plugin-helm/tsconfig.app.json",
        "outputPath": "build/plugins/plugin-helm",
        "format": ["cjs"]
      }
    }
  }
}
